{
  "name": "@kambrasil/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API do Kam Brasil — contas, autenticação e lista de servidores",
  "scripts": {
    "dev": "bun --watch --env-file=../.env src/server.ts",
    "start": "bun --env-file=../.env src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@fastify/jwt": "^10.2.0",
    "@node-rs/argon2": "^2.0.2",
    "fastify": "^5.10.0",
    "pg": "^8.22.0",
    "reflect-metadata": "^0.2.2",
    "typeorm": "^1.1.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "pino-pretty": "^13.1.3",
    "typescript": "^7.0.2"
  }
}
